import { Observable } from 'rxjs/Observable';
import { ScrollToConfig } from '../models/scroll-to-config.model';
import { ScrollToListenerTarget } from '../models/scroll-to-targets.model';
export declare class ScrollToAnimation {
    private _container;
    private _listenerTarget;
    private readonly _is_window;
    private readonly _to;
    private readonly _options;
    private _is_browser;
    private _tick;
    private _interval;
    private _time_lapsed;
    private _percentage;
    private _position;
    private _start_position;
    private _distance;
    private _source$;
    private _windowScrollTop;
    constructor(_container: HTMLElement, _listenerTarget: ScrollToListenerTarget, _is_window: boolean, _to: number, _options: ScrollToConfig, _is_browser: boolean);
    /**
     * Start the new Scroll Animation.
     *
     * @todo consider using enums for actiontypes
     *
     * @returns void
     */
    start(): Observable<any>;
    /**
     * Recursively loop over the Scroll Animation.
     *
     * @returns void
     */
    private _loop;
    /**
     * Stop the current Scroll Animation Loop.
     *
     * @param force 			Force to stop
     */
    stop(force?: boolean): void;
}
